set(TD1_SRC




)

set(TD1_LIB






)

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${BIN}/TD1)

# add_library(TD1 ${TD1_LIB})

# foreach(s ${TD2_SRC})
#     add_executable(${s} "${s}.c")
#     target_link_libraries(${s} TD2)
# endforeach(s)